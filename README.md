# proyecciones-pruebas-modelos
## Requisitos

- Python 3.10 – 3.12
- Ejecución en CPU (CUDA desactivado explícitamente)

## Instalación

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
